services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: nframa
      POSTGRES_PASSWORD: nframa
      POSTGRES_DB: nframa
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
